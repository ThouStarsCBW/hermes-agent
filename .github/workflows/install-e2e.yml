# DISABLED WORKFLOW - 该工作流已被禁用
# 原始工作流: Install & Update E2E
# 禁用原因: 用户主动禁用
# 禁用日期: 2026-08-17
#
# 如需恢复，请删除上述注释并从版本控制历史中恢复原始内容

name: Install & Update E2E (DISABLED)

on:
  # 已禁用所有触发器
  workflow_dispatch: {}

# 空的jobs配置使工作流不执行任何操作
jobs:
  disabled:
    name: This workflow is disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Install & Update E2E workflow is currently disabled"
